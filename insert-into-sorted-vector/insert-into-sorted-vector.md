# Environment

- A self defined class `Item`
- `std::vector<Item>`

# We make use of
- `std::sort`
- `std::lower_bound` (also we can use `upper_bound` for strict greater)
- `vector.insert`

# Time Complexity
$O(NlogN)$